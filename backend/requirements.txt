fastapi
uvicorn[standard]
python-multipart
pywebpush>=2.0.0
cryptography>=42
watchdog>=4

# dev / lint / test (= task lint, task test:unit)
flake8>=7
black>=24
pytest>=8
httpx  # starlette TestClient (= backend/tests/) が実行時に要求する
